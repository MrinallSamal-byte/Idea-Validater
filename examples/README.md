# Examples

Each file shows the same prompt answered two ways: a normal, agreeable assistant
versus Reality Check running the protocol. The point isn't to be negative — it's
to surface the load-bearing assumption and commit to a verdict.
